{{ form_start(form) }}
<div class="container youplay-content">
    <div class="col-md-12">
        <div class="row">
            <div class="col-md-6">
                <p>Os:</p>
                <div class="youplay-input">
                    {{ form_widget(form.os, {'attr': {'class': 'form-control', 'placeholder': 'Os'}}) }}
                </div>
            </div>
            <div class="col-md-6">
                <p>Processor:</p>
                <div class="youplay-input">
                    {{ form_widget(form.processor, {'attr': {'class': 'form-control', 'placeholder': 'Processor'}}) }}
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-md-6">
                <p>Memory:</p>
                <div class="youplay-input">
                    {{ form_widget(form.memory, {'attr': {'class': 'form-control', 'placeholder': 'Memory'}}) }}
                </div>
            </div>
            <div class="col-md-6">
                <p>Graphics:</p>
                <div class="youplay-input">
                    {{ form_widget(form.graphics, {'attr': {'class': 'form-control', 'placeholder': 'Graphics'}}) }}
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-md-6">
                <p>DirectX:</p>
                <div class="youplay-input">
                    {{ form_widget(form.directX, {'attr': {'class': 'form-control', 'placeholder': 'DirectX'}}) }}
                </div>
            </div>
            <div class="col-md-6">
                <p>Network:</p>
                <div class="youplay-input">
                    {{ form_widget(form.network, {'attr': {'class': 'form-control', 'placeholder': 'Network'}}) }}
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-md-6">
                <p>Hard Drive:</p>
                <div class="youplay-input">
                    {{ form_widget(form.hardDrive, {'attr': {'class': 'form-control', 'placeholder': 'Hard Drive'}}) }}
                </div>
            </div>
            <div class="col-md-6">
                <p>Sound Card:</p>
                <div class="youplay-input">
                    {{ form_widget(form.soundCard, {'attr': {'class': 'form-control', 'placeholder': 'Sound Card'}}) }}
                </div>
            </div>
        </div>
    </div>
</div>
    <button class="btn btn-default btn-lg pull-right">{{ button_label|default('Submit') }}</button>
{{ form_end(form) }}