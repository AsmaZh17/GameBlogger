{% extends 'baseAdmin.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
<section class="youplay-banner banner-top youplay-banner-parallax small">
    <div class="image" data-speed="0.4">
        <img src="{{ asset('images/dark/banner-blog-bg.jpg') }}" alt="" class="jarallax-img">
    </div>
    <div class="info">
        <div>
            <div class="container">
                <h1 class="h1">Categorie</h1>
            </div>
        </div>
    </div>
</section>
<div class="container youplay-content">
    <table class="table table-bordered table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categorie.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ categorie.description }}</td>
            </tr>
        </tbody>
    </table>
</div>
 
    <a href="{{ path('app_categorie_index') }}" class="btn btn-lg pull-left">back to list</a>

    <a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}" class="btn btn-lg pull-right">edit</a>

    {{ include('admin/categorie/_delete_form.html.twig') }}

{% endblock %}
